# CHG-0001 Evidence

## Claim

The two ChatGPT exporter entry buttons are separated by a minimal generic-script-only positioning change, while their existing DOM creation and click paths remain intact.

## Root Cause

Both scripts independently append one fixed button to `document.body` in the bottom-right region. The generic script used `right: 20px; bottom: 20px; zIndex: 99999`; ChatGPT v0.4 used `right: 24px; bottom: 24px; zIndex: 99997`. The differing z-index values do not prevent geometric overlap.

## Change

Changed only the generic script's button anchor from `bottom: "20px"` to `bottom: "80px"`. Right alignment, z-index, button creation, click handler, dialog invocation, and export flow are unchanged.

## Method

- Inspected current UI creation and positioning in both scripts.
- Applied a one-line source change to `通用AI对话导出脚本.txt`.
- Ran Node syntax checks through standard input and inspected the source diff.

## Result

PASS. Static syntax and structural inspection passed, and the Human Owner verified on the live ChatGPT page that both entry buttons remain visible, do not overlap, remain usable after zooming, are independently clickable, and open their functions normally.

## Acceptance Mapping

| Criterion | Result | Evidence |
|---|---|---|
| Both entry paths remain in code | PASS | Both `mountButton()` and `addBtn()` remain present. |
| Independent click handlers remain | PASS | Existing listeners/handlers are unchanged. |
| Default desktop no overlap | PASS | Human Owner verified on the live ChatGPT page, including zoom. |
| No new duplicate DOM / obvious UI regression | PASS static | One CSS property changed; no DOM code changed. |
| Existing export paths preserved | PASS static | No export or handler logic changed. |
| Offline boundary preserved | PASS | Static and real-site evidence are distinguished. |

## Limitations

The real-site verification is based on Human Owner acceptance; no automated browser trace or download artifact was captured in this session.
